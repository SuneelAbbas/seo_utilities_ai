runtime:
  image: node:22

commands:
  package: ""
  build:
    - "npm install"
    - "npm run install:muffet"
    - "npm run build"
  start: "npm run start"

repository:
  branch: main
  auto_deploy: true
